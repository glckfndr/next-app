"use client";
import React from "react";

const AddToCart = () => {
  return (
    <button onClick={() => console.log("Button is pressed")}>
      Add to cart
    </button>
  );
};

export default AddToCart;
